Really good spot, UI is pretty solid, just a few small "bugs" like:

So i ran a test and it caught the 2 images correctly, I changed the name on one file, but did not click 'rename files on disk', clicked on the next file to change the name of that one, and the first file disappeared.